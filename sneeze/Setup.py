class Setup:
    window_size = (1600, 1200)
    logical_size = (1600, 1200)
    # scale = (
    #     logical_size[0] / window_size[0],
    #     logical_size[1] / window_size[1]
    # )
    fps = 30
    # speed = 4
